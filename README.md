# MapLab 🗺️

**MapLab** — это интерактивное мобильное приложение для создания маркеров на карте с возможностью добавления изображений. Приложение разработано с использованием Expo, React Native и TypeScript.

## ✨ Основные возможности

- 🗺️ **Интерактивная карта** с возможностью размещения маркеров
- 📍 **Создание маркеров** длинным нажатием на карту
- ❌ **Удаление маркеров** с подтверждением в экране деталей
- 📷 **Добавление изображений** к маркерам из галереи устройства
- 📱 **Детальный просмотр** маркеров с информацией и изображениями
- 🔒 **Строгая типизация** с TypeScript
- ⚡ **Оптимизированная производительность** с мемоизацией компонентов

## 🚀 Инструкции по установке

### Предварительные требования

- **Node.js** (версия 18.0.0 или выше) - [Скачать](https://nodejs.org/)
- **npm** или **yarn** (поставляется с Node.js)
- **Expo CLI** - устанавливается глобально: `npm install -g @expo/cli`
- **iOS Simulator** (для macOS) или **Android Studio** с эмулятором
- **Expo Go** на мобильном устройстве (опционально)

### Пошаговая установка

1. **Клонирование репозитория**
   ```bash
   git clone <repository-url>
   cd MapLab
   ```

2. **Установка зависимостей**
   ```bash
   # С использованием npm
   npm install
   
   # С использованием yarn
   yarn install
   ```

3. **Проверка зависимостей (опционально)**
   ```bash
   npx expo doctor
   ```

4. **Запуск приложения**
   ```bash
   # Запуск в режиме разработки
   npm start
   # или
   yarn start
   
   # Запуск с очисткой кеша (при проблемах)
   npx expo start -c
   ```

5. **Открытие приложения**
   - **iOS Simulator**: нажмите `i` в терминале
   - **Android Emulator**: нажмите `a` в терминале  
   - **Веб-браузер**: нажмите `w` в терминале
   - **Физическое устройство**: отсканируйте QR-код в приложении Expo Go

## 📱 Использование

1. **Создание маркера**: Сделайте длинное нажатие на любом месте карты
2. **Просмотр маркера**: Нажмите на существующий маркер для перехода к деталям
3. **Добавление изображений**: В экране деталей маркера нажмите "Добавить фото"
4. **Удаление изображений**: Нажмите на крестик в углу изображения или сделайте длинное нажатие
5. **Удаление маркера**: В экране деталей маркера нажмите красную кнопку "Удалить маркер" (с подтверждением)

## 🗄️ Документация схемы базы данных

### Обзор базы данных

Приложение использует **SQLite** в качестве локальной базы данных через **expo-sqlite**. База данных автоматически инициализируется при первом запуске приложения.

### Схема таблиц

#### Таблица `markers`
Хранит основную информацию о маркерах на карте.

```sql
CREATE TABLE markers (
  id TEXT PRIMARY KEY,              -- Уникальный идентификатор маркера
  latitude REAL NOT NULL,           -- Широта (координата Y)
  longitude REAL NOT NULL,          -- Долгота (координата X)
  title TEXT NOT NULL DEFAULT '',   -- Заголовок маркера
  description TEXT NOT NULL DEFAULT '', -- Описание маркера
  created_at TEXT NOT NULL,         -- Дата создания (ISO строка)
  updated_at TEXT NOT NULL          -- Дата последнего обновления (ISO строка)
);
```

#### Таблица `images`
Хранит информацию об изображениях, привязанных к маркерам.

```sql
CREATE TABLE images (
  id TEXT PRIMARY KEY,              -- Уникальный идентификатор изображения
  marker_id TEXT NOT NULL,          -- ID маркера (внешний ключ)
  uri TEXT NOT NULL,                -- Путь/URI к изображению
  name TEXT NOT NULL,               -- Имя файла изображения
  date_added TEXT NOT NULL,         -- Дата добавления (ISO строка)
  size INTEGER,                     -- Размер файла в байтах (опционально)
  type TEXT,                        -- MIME тип изображения (опционально)
  FOREIGN KEY (marker_id) REFERENCES markers (id) ON DELETE CASCADE
);
```

### Индексы для оптимизации

```sql
-- Индекс для быстрого поиска изображений по маркеру
CREATE INDEX idx_images_marker_id ON images (marker_id);

-- Индекс для сортировки маркеров по дате создания
CREATE INDEX idx_markers_created_at ON markers (created_at);
```

### Особенности реализации

- **Каскадное удаление**: При удалении маркера автоматически удаляются все связанные изображения
- **Транзакции**: Все операции с несколькими таблицами выполняются в транзакциях
- **Миграции**: Схема базы данных версионируется и обновляется автоматически
- **Внешние ключи**: Включена поддержка foreign keys для обеспечения целостности данных

## ⚠️ Описание подхода к обработке ошибок

### Централизованная система обработки ошибок

Приложение использует многоуровневую систему обработки ошибок для обеспечения надежности и хорошего пользовательского опыта.

#### 1. Типизированные ошибки

```typescript
export type ErrorType =
  | "image"      // Ошибки работы с изображениями
  | "navigation" // Ошибки навигации
  | "map"        // Ошибки карты
  | "general"    // Общие ошибки
  | "permission" // Ошибки разрешений
  | "database"   // Ошибки базы данных
  | "transaction" // Ошибки транзакций
  | "connection"; // Ошибки соединения

export interface ErrorState {
  readonly message: string;    // Сообщение для пользователя
  readonly type: ErrorType;    // Категория ошибки
  readonly timestamp?: Date;   // Время возникновения
  readonly details?: string;   // Дополнительные детали
}
```

#### 2. Компонент ErrorHandler

Централизованный компонент для отображения ошибок:

```typescript
// components/ErrorHandler.tsx
export default function ErrorHandler({ error, onDismiss }: ErrorHandlerProps) {
  if (!error) return null;
  
  return (
    <View style={styles.container}>
      <Text style={styles.message}>{error.message}</Text>
      <TouchableOpacity style={styles.button} onPress={onDismiss}>
        <Text style={styles.buttonText}>Закрыть</Text>
      </TouchableOpacity>
    </View>
  );
}
```

#### 3. Обработка на уровне хранилища (Store)

Все операции в Zustand store обернуты в try-catch блоки:

```typescript
// Пример из store/markers.ts
addMarker: async (data: CreateMarkerData): Promise<string | null> => {
  try {
    set({ isLoading: true, error: null });
    const markerId = await markersRepository.createMarker(data);
    await get().loadMarkers();
    set({ isLoading: false, error: null });
    return markerId;
  } catch (error) {
    const errorState = handleError(error, ERROR_MESSAGES.MARKER_ADD_FAILED);
    set({ error: errorState, isLoading: false });
    return null;
  }
}
```

#### 4. Утилиты для обработки ошибок

```typescript
// utils/helpers.ts
export const handleError = (
  error: unknown,
  fallbackMessage: string
): ErrorState => {
  const message = error instanceof Error ? error.message : fallbackMessage;
  console.error("Application Error:", error);
  
  return {
    type: "general",
    message,
    timestamp: new Date(),
  };
};
```

#### 5. Обработка ошибок базы данных

Специальная обработка с повторными попытками:

```typescript
// database/service.ts
async executeTransaction(transaction: DatabaseTransaction): Promise<void> {
  let lastError: DatabaseError | null = null;
  
  for (let attempt = 1; attempt <= DB_CONSTANTS.MAX_RETRY_ATTEMPTS; attempt++) {
    try {
      // ... выполнение транзакции
      return;
    } catch (error) {
      lastError = this.createDatabaseError(
        `Transaction failed (attempt ${attempt})`,
        error
      );
      if (attempt < DB_CONSTANTS.MAX_RETRY_ATTEMPTS) {
        await this.delay(DB_CONSTANTS.RETRY_DELAY * attempt);
      }
    }
  }
  throw lastError;
}
```

#### 6. Стратегии восстановления

- **Повторные попытки**: Автоматические повторы для операций с базой данных
- **Откат состояния**: При ошибке состояние возвращается к предыдущему
- **Graceful degradation**: Приложение продолжает работать даже при частичных ошибках
- **Пользовательские уведомления**: Понятные сообщения об ошибках на русском языке

## ✨ Дополнительно реализованные возможности

### 1. Производительность и оптимизация

- **React.memo**: Все компоненты мемоизированы для предотвращения лишних ререндеров
- **useCallback/useMemo**: Мемоизация функций и вычислений в хуках
- **Lazy loading**: Асинхронная загрузка изображений с оптимизацией памяти
- **Виртуализация**: Оптимизированное отображение списков изображений

### 2. Пользовательский интерфейс

- **Адаптивный дизайн**: Интерфейс адаптируется под размеры экрана
- **Темная тема**: Автоматическое определение системной темы
- **Haptic feedback**: Тактильная обратная связь на iOS
- **Анимации**: Плавные переходы между экранами с react-native-reanimated

### 3. Работа с изображениями

- **Автоматическое сжатие**: Изображения сжимаются до оптимального размера
- **Предварительный просмотр**: Превью изображений в сетке
- **Множественное удаление**: Удаление нескольких изображений одновременно
- **Кеширование**: Автоматическое кеширование изображений с expo-image

### 4. Карта и геолокация

- **Пользовательская локация**: Отображение текущего местоположения
- **Жесты карты**: Поддержка всех стандартных жестов (zoom, pan, rotate)
- **Кастомные маркеры**: Возможность настройки внешнего вида маркеров
- **Регионы**: Автоматическое определение оптимального региона для отображения

### 5. Типобезопасность

- **Строгий TypeScript**: Максимально строгие настройки компилятора
- **Типизированная навигация**: Автогенерация типов для Expo Router
- **Readonly типы**: Неизменяемые структуры данных для предотвращения багов
- **Утилитарные типы**: Переиспользуемые типы для частых паттернов

### 6. Разработческий опыт

- **Hot reload**: Мгновенное обновление при изменении кода
- **ESLint конфигурация**: Автоматическая проверка качества кода
- **TypeScript строгий режим**: Максимальная проверка типов
- **Логирование**: Подробное логирование операций базы данных в режиме разработки

## 🛠️ Выбор ORM и детали реализации

### Почему не используется ORM?

В данном проекте принято решение **НЕ использовать ORM** по следующим причинам:

#### Обоснование решения

1. **Простота схемы**: Приложение использует всего 2 таблицы с простыми связями
2. **Производительность**: Прямые SQL запросы быстрее ORM запросов
3. **Контроль**: Полный контроль над SQL запросами и оптимизациями
4. **Размер приложения**: Отсутствие дополнительных зависимостей
5. **Обучающая цель**: Демонстрация работы с чистым SQL

#### Repository Pattern

```typescript
// database/markersRepository.ts
export class MarkersRepository {
  async createMarker(data: CreateMarkerData): Promise<string> {
    // Прямой SQL запрос с параметрами
    const transaction: DatabaseTransaction = {
      queries: [{
        sql: `INSERT INTO markers (...) VALUES (?, ?, ?, ?, ?, ?, ?)`,
        args: [markerId, ...data]
      }]
    };
    await databaseService.executeTransaction(transaction);
    return markerId;
  }
}
```

#### Service Layer

```typescript
// database/service.ts
class DatabaseService {
  async executeQuery<T>(sql: string, params?: readonly any[]): Promise<T[]> {
    // Обертка над expo-sqlite с обработкой ошибок
    // Логирование, повторные попытки, валидация
  }
}
```

#### Преимущества текущего подхода

- **Типобезопасность**: TypeScript интерфейсы для всех моделей данных
- **Миграции**: Собственная система версионирования схемы
- **Транзакции**: Полная поддержка ACID транзакций
- **Производительность**: Оптимизированные запросы без ORM overhead
- **Отладка**: Прозрачность всех выполняемых запросов

## 🏗️ Архитектура приложения

### Структура проекта

```text
MapLab/
├── app/                    # Страницы приложения (Expo Router)
│   ├── (tabs)/            # Табы навигации
│   │   └── index.tsx      # Главная страница с картой
│   ├── marker/            # Детали маркера
│   │   └── [id].tsx       # Динамический маршрут для маркера
│   └── _layout.tsx        # Корневой layout с навигацией
├── components/            # Переиспользуемые компоненты
│   ├── Map.tsx           # Компонент карты с маркерами
│   ├── ImageList.tsx     # Список изображений с управлением
│   └── ErrorHandler.tsx  # Централизованный обработчик ошибок
├── constants/            # Константы и конфигурация
│   └── config.ts         # Основная конфигурация приложения
├── database/             # Слой работы с базой данных
│   ├── config.ts         # Конфигурация и миграции БД
│   ├── service.ts        # Сервис для работы с SQLite
│   ├── markersRepository.ts # Репозиторий для маркеров
│   └── index.ts          # Точка входа для БД
├── hooks/                # Пользовательские React хуки
│   ├── use-markers.ts    # Хук для работы с маркерами
│   ├── use-images.ts     # Хук для работы с изображениями
│   └── use-database.ts   # Хук инициализации БД
├── store/                # Управление состоянием (Zustand)
│   ├── markers.ts        # Store маркеров с бизнес-логикой
│   ├── images.ts         # Store изображений
│   └── index.ts          # Экспорты store
├── styles/               # Общие стили и темы
│   └── common.ts         # Переиспользуемые стили
├── utils/                # Утилитарные функции
│   └── helpers.ts        # Вспомогательные функции
├── types.ts              # Глобальные TypeScript типы
└── README.md             # Документация проекта
```

### Принятые архитектурные решения

#### 1. State Management - Zustand

**Почему выбрано:**
- Легковесная альтернатива Redux (2KB vs 50KB+)
- Отличная интеграция с TypeScript
- Middleware для подписок и персистентности

**Реализация:**
- Разделение на логические store (markers, images)
- Использование subscribeWithSelector middleware
- Строгая типизация всех действий и состояний

#### 2. Navigation - Expo Router

**Почему выбрано:**
- Файловая система маршрутизации (Next.js подход)
- Автоматическая генерация типов для навигации
- Встроенный deep linking и универсальные ссылки
- Оптимизация для мобильных устройств

**Реализация:**
- Структура `(tabs)` для основной навигации
- Динамические маршруты `[id]` для деталей
- Типобезопасные параметры маршрутов

#### 3. Типизация - Строгий TypeScript

**Конфигурация:**
```json
{
  "strict": true,
  "noImplicitAny": true,
  "strictNullChecks": true,
  "exactOptionalPropertyTypes": true,
  "noUncheckedIndexedAccess": true
}
```

**Подходы:**
- Readonly типы для предотвращения мутаций
- Утилитарные типы для переиспользования
- Строгие интерфейсы для API и БД

#### 4. База данных - SQLite без ORM

**Архитектура:**
- Service Layer для низкоуровневых операций
- Система миграций с версионированием
- Транзакционная безопасность

## ⚠️ Известные проблемы и ограничения

### Текущие ограничения

1. **Производительность карты**
   - При большом количестве маркеров возможны задержки
   - Планируется кластеризация маркеров

2. **Размер изображений**  
   - Максимальный размер файла ограничен 5MB
   - Нет автоматического сжатия

3. **Offline режим**
   - Требуется интернет для отображения карты
   - Планируется кеширование тайлов

4. **Веб-ограничения**
   - Не все нативные API доступны в веб-версии
   - Ограниченный функционал на десктопе